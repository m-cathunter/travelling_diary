<head>
  <h3>
    <%= @trip.city %>
  </h3>
</head>

<%= @trip.description %>

<hr/>

<%= link_to "Edit", edit_trip_path(@trip), class: "btn btn-primary" %>
<%= link_to "Remove", trip_path(@trip), class: "btn btn-danger",
  method: :delete, data: { confirm: "Are you sure?"}%>
